1.vim--编辑
'u' : 撤销上一个编辑操作

'ctrl + r' : 恢复，即回退前一个命令

'U' : 行撤销，撤销所有在前一个编辑行上的操作


2.vim - 退出
	
执行以下命令之前可以先按一下  Esc  键，以确保处于视图模式
：命令严格区分大小写
- 保存并退出       :wq + 回车键
- 保存并退出       ZZ
- 不保存退出       :q! +回车键